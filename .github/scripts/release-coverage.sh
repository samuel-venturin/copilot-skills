#!/usr/bin/env bash
set -euo pipefail

# This repository is a collection of Copilot CLI skills and Node.js tooling
# scripts, not an application with automated test coverage. Report as not
# applicable rather than running a test suite that doesn't exist.

echo "COVERAGE_LINES=n/a"
echo "COVERAGE_HIT=0"
echo "COVERAGE_FOUND=0"
